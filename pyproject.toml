[project]
name = "zer0gear Comfy Utilities"
description = "Nodes: ListConcat with custom separator"
version = "1.0.0" # Custom Node version. Must be semantically versioned.
license = { file = "LICENSE.txt" }
dependencies  = [] # Filled in from requirements.txt

[project.urls]
Repository = "https://github.com/zer0thgear/zer0-comfy-utils"

[tool.comfy]
PublisherId = "zer0gear" # TODO (fill in Publisher ID from Comfy Registry Website).
DisplayName = "zer0ComfyUtilities" # Display name for the Custom Node. Can be changed later.
Icon = "https://example.com/icon.png" # SVG, PNG, JPG or GIF (MAX. 800x400px)